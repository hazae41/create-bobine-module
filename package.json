{
  "type": "module",
  "scripts": {
    "prepack": "rm -rf ./out && asc ./src/bin.ts -o ./out/bin.wasm --runtime stub --enable reference-types",
    "produce": "npm run prepack && deno run -A ./run/src/mods/produce/mod.ts ./out/bin.wasm",
    "execute": "deno run -A ./run/src/mods/execute/mod.ts"
  },
  "devDependencies": {
    "@hazae41/stdbob": "^0.0.12",
    "assemblyscript": "^0.28.9"
  }
}